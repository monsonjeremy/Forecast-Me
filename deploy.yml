version: '2'
  services:
    app:
      image: monsonjeremy/forecast-me
      ports:
        - "80:80"
    redis:
      image: redis:4.0.2-alpine
      volumes: /data:/data
      ports:
        - "6379:6379"